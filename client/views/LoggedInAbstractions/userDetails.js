//Template.userDetails.events({
//	'click #nextUser': function(e, tmpl) {
//		Session.set("buttonClicked", this.Date()); 
		//Create a session variable that is changed on buttonClick which will fire up this even and embedding this into the template will allow bootlegged refresh to templates.
//	}
//});

//Template.userDetails.buttonClicked = function() {
//	return Session.get('buttonClicked');
//};